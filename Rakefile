# frozen_string_literal: true

# Add your own tickets in files placed in lib/tickets ending in .rake,
# for example lib/tickets/capistrano.rake, and they will automatically be available to Rake.

require_relative 'config/application'

Rails.application.load_tasks
